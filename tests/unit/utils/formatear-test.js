import formatear from 'pilas-engine/utils/formatear';
import { module, test } from 'qunit';

module('Unit | Utility | formatear');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatear();
  assert.ok(result);
});
