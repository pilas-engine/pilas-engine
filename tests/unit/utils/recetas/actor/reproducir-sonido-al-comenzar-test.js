import recetasActorReproducirSonidoAlComenzar from 'pilas-engine/utils/recetas/actor/reproducir-sonido-al-comenzar';
import { module, test } from 'qunit';

module('Unit | Utility | recetas/actor/reproducir-sonido-al-comenzar', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = recetasActorReproducirSonidoAlComenzar();
    assert.ok(result);
  });
});
