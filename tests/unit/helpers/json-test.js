
import { json } from 'pilas-engine/helpers/json';
import { module, test } from 'qunit';

module('Unit | Helper | json', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = json([42]);
    assert.ok(result);
  });
});

