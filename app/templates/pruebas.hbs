{{outlet}}

<div class="h-100 flex">
  {{#if ocultar_canvas}}
    Cargando ...
  {{else}}
    {{pilas-canvas
      ancho=proyecto.ancho
      alto=proyecto.alto
      proyecto=proyecto
      modoZoom=2
      cuando_termina_de_cargar=(action "cuando_termina_de_cargar")
    }}
  {{/if}}
</div>
