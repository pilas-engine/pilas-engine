{{#pilas-tooltip class="right-1 w4" texto="Tamaño de la grilla"}}
  <select class="w-100 select f7 mt1 verdana" onchange={{action "cuandoCambia" value="target.value"}} >
    {{#each items as |item|}}
      <option value={{item.valor}} selected={{eq grilla item.valor}}>{{item.etiqueta}}</option>
    {{/each}}
  </select>
{{/pilas-tooltip}}
